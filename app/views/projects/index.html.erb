<div class="content">
  <div class="maincontent">
    <h1>All Projects</h1>
    <div class="list">
      <ul>
        <% @projects.each do |project| %>
        <li>
        <%= link_to project.name, project_path(project) %>
        </li><br>
        <% end %>
      </ul>
    </div>
  </div>
</div>

