<div class="topbar">VolunTravel | 
See the world.
Do good.</div>

<div class="content">
  <h1><%= @location.name %></h1>
  <%= @location.description %>
  <br>

  <div class="list">
    <h3>Projects at this Location:</h3>
    <ul>
    <% @location.projects.each do |project| %>
    <li>
    <%= link_to project.name, project_path(project) %>
    </li>
    <% end %>
    </ul>
  </div>

  <div class="returnhome">
  <%= render '/layouts/home_link' %>
  </div>
</div>

<div class="bottombar">
Travel. Explore. Learn. Do good.
</div>