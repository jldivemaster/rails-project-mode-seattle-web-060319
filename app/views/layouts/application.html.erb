<!DOCTYPE html>
<html>
  <head>
    <title>VolunteerProjects</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', 'home', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <div class="topbar">
      <% if current_user %>
        VolunTravel | See the world. Do good.
        <!-- current_user will return true if a user is logged in -->
        <div class="navbar"><%= "Logged in as #{current_user.username}" %> | <%= link_to 'Home', root_path %> | <%= link_to 'My Profile', user_path(current_user) %> | <%= link_to 'Log Out', logout_path, method: :delete %></div>
      <% else %>
        <!-- not logged in -->
        VolunTravel | See the world. Do good.
        <div class="navbar"><%= link_to 'Home', root_path %> | <%= link_to 'Log In', login_path %> or <%= link_to 'Sign Up', new_user_path %></div>
      <% end %>
    </div>


     <!-- flash message -->
    <% if notice %>
        <%= notice %>
    <% end %>

    <% if alert %>
      <%= alert %>
    <% end %>



    <%= yield %>

    <div class="bottombar">
      Travel. Explore. Learn.
    </div>

  </body>
</html>
